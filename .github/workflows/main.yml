# name: Cypress Tests

# on:
#   push:
#   workflow_dispatch:

# jobs:
#   cypress-run:
#     runs-on: ubuntu-24.04
#     strategy:
#       fail-fast: false
#       matrix:
#         browser: [chrome]

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Install Dependencies
#         run: npm ci

#       - name: Run Cypress Tests
#         run: |
#           npx cypress run --browser ${{ matrix.browser }}

#       - name: Merge Mochawesome JSON Reports
#         run: |
#           npx mochawesome-merge cypress/reports/*.json > cypress/reports/merged-report.json
#         continue-on-error: true

#       - name: Generate HTML Report
#         run: |
#           if [ -f cypress/reports/merged-report.json ]; then
#             npx mochawesome-report-generator cypress/reports/merged-report.json -o cypress/reports
#           else
#             echo "Merged JSON not found, skipping HTML generation."
#           fi
#         continue-on-error: true

#       - name: Upload Cypress Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: Cypress Artifacts
#           path: |
#             cypress/screenshots
#             cypress/videos
#             cypress/reports


# without mochawsome reporter correct code
# name: Cypress Tests

# on:
#   push:
#   workflow_dispatch:

# jobs:
#   cypress-run:
#     runs-on: ubuntu-24.04
#     strategy:
#       fail-fast: false
#       matrix:
#         browser: [chrome]

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Install Dependencies
#         run: npm ci

#       - name: Run Cypress Tests
#         run: npx cypress run --browser ${{ matrix.browser }}

#       - name: Upload Cypress Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: Cypress Artifacts
#           path: |
#             cypress/screenshots
#             cypress/videos


name: Cypress Tests

on:
  push:
  workflow_dispatch:

jobs:
  cypress-run:
    runs-on: ubuntu-24.04

    steps:
      # Checkout the repository
      - name: Checkout
        uses: actions/checkout@v4

      # Install dependencies
      - name: Install Dependencies
        run: npm ci

      # Create reports folder
      - name: Create reports folder
        run: mkdir -p cypress/reports

      # Run Cypress tests
      - name: Run Cypress Tests
        run: |
          npx cypress run \
            --browser chrome \
            --reporter cypress-mochawesome-reporter \
            --reporter-options reportDir=cypress/reports,json=true,html=true,overwrite=false
        continue-on-error: true

      # Merge only non-empty Mochawesome JSON reports
      - name: Merge Mochawesome JSON Reports
        run: |
          files=$(find cypress/reports -name '*.json' -size +0)
          if [ -n "$files" ]; then
            npx mochawesome-merge $files > cypress/reports/merged-report.json
          else
            echo "No JSON files to merge, skipping."
          fi
        continue-on-error: true

      # Generate HTML report safely
      - name: Generate HTML Report
        run: |
          if [ -f cypress/reports/merged-report.json ] && [ -s cypress/reports/merged-report.json ]; then
            npx mochawesome-report-generator cypress/reports/merged-report.json -o cypress/reports
          else
            echo "Merged JSON not found or empty, skipping HTML generation."
          fi
        continue-on-error: true

      # Upload screenshots, videos, and reports
      - name: Upload Cypress Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Cypress Artifacts
          path: |
            cypress/screenshots
            cypress/videos
            cypress/reports
